apiVersion: v1
kind: Service
metadata:
  name: cicdi-config-center-web-svc
  namespace: platform
  labels:
    app: cicdi-config-center-web-svc
spec:
  type: NodePort
  sessionAffinity: ClientIP
  ports:
    - port: 80
      name: business
      targetPort: 80
      nodePort: 49999
  selector:
    app: cicdi-config-center-web-app
---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: cicdi-config-center-web-dep
  namespace: platform
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: cicdi-config-center-web-app
    spec:
      containers:
      - name: cicdi-config-center-web-app
        image: 172.16.12.236/platform/cicdi-config-center-web-image:20190410141734
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: 500Mi
          limits:
            memory: 1024Mi
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/conf.d/
      volumes:
      - name: nginx-config
        configMap:
          name: cicdi-config-center-web-configmap
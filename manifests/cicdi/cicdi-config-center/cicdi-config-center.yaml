apiVersion: v1
kind: Service
metadata:
  name: cicdi-config-center-svc
  labels:
    app: cicdi-config-center-svc
spec:
  type: NodePort
  sessionAffinity: ClientIP
  ports:
    - port: 8080
      name: business
      targetPort: 8080
      nodePort: 30005
    - port: 5005
      name: debug
      targetPort: 5005
      nodePort: 20005
  selector:
    app: cicdi-config-center-app
---
apiVersion: v1
kind: Service
metadata:
  name: cicdi-config-center-remote-svc
  labels:
    app: cicdi-config-center-remote-svc
spec:
  sessionAffinity: ClientIP
  ports:
    - port: 18082
      name: remote
      targetPort: 18082
  selector:
    app: cicdi-config-center-app
---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: cicdi-config-center-dep
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: cicdi-config-center-app
    spec:
      containers:
      - name: cicdi-config-center-app
        image: 172.16.12.236/platform/cicdi-config-center:20190418100702dailyupdate
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: 400Mi
          limits:
            memory: 2560Mi